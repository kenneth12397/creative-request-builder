import React from "react";
import { createRoot } from "react-dom/client";
import CreativeRequestBuilderPrototype from "../creative_brief_builder_prototype.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CreativeRequestBuilderPrototype />
  </React.StrictMode>
);
